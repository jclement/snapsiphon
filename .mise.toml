[tasks.release]
description = "Cut a release: mise run release -- 0.2.1 (clean-tree check, tag-monotonic check, archive with version+build+githash, tag & push)"
run = 'scripts/release.sh {{arg(name="version")}}'
